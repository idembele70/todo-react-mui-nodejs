import { createTheme } from "@material-ui/core"

const theme = createTheme({
  palette: {
    marine: {
      main: "#094067"
    },
    primary: {
      main: "#3DA9FC",
      light: "#2196f375"
    },
    secondary: {
      main: "#FFF"
    },
    red: {
      main: "#EF4565",
      light: "#f1607b"
    }
  }
})
export default theme
